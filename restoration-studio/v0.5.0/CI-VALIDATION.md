# CI validation

This file records the pull-request validation gate for Restoration Studio Cloud v0.5.0.

The workflow must assemble the readable frontend release, run JavaScript syntax checks, install FFmpeg and the Personal Worker, execute recovery and end-to-end tests, and pass dependency consistency checks before this change is merged.
